using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuickSlot_Manager : MonoBehaviour
{
    public Transform[] quickSlots;

    private void Update()
    {
        //if (quickSlots[0].childCount == 1) return;

        //if (quickSlots[0].GetChild(1).gameObject.activeSelf == false) return;

        //Debug.Log(quickSlots[0].GetChild(1).GetComponent<Item_Action>().m_itemInfo.itemSprite.name);
    }
}
